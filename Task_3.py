  # import turtle,below code is meant to draw flower shape
import turtle

 # create the pattern for the turtle
pat =turtle.Turtle()
  # select the background colour 
scr.bgcolour("black")
pat.speed(0) 
  # set pattern speed

radius=60
pet.pensize(2)
  # select the colour of the shape
colour = ["red","green","purple"] 

  
for i in range(6):
    pat.circle(radius)
    pat.right(60)
    if i<0:
       break

turtle.done()
